[phases.setup]
nixPkgs = ["python311"]

[phases.install]
# यहाँ हम पहले लाइब्रेरी इंस्टॉल करेंगे, फिर ब्राउज़र और उसकी डिपेंडेंसी
cmds = [
    "pip install -r requirements.txt",
    "playwright install chromium",
    "playwright install-deps"
]

[phases.start]
cmds = ["python main.py"]
